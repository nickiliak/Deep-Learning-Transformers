services:
  db:
    image: ankane/pgvector:latest
    environment:
      POSTGRES_USER: nick
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: vectordb
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  env_writer:
    image: python:3.11-alpine
    depends_on:
      - db
    volumes:
      - ./:/workspace
    working_dir: /workspace
    entrypoint: ["sh", "-c"]
    command: ["sh", "-c", "sleep 5; echo \"DATABASE_URL=postgresql+psycopg://nick:secret@localhost:5432/vectordb\" > .env; echo WROTE .env"]

volumes:
  pgdata: {}
